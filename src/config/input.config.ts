/** Controles de toque (celular). */
export const touchConfig = {
  /** Mira assistida ao atirar: procura zumbis neste cone em volta da lanterna (graus para cada lado). */
  assistConeDeg: 22,
  /** Distância máxima do alvo da mira assistida (px). */
  assistRange: 430,
  /** Quanto a lanterna gira em direção ao alvo por frame (0..1; 1 = instantâneo). */
  assistTurn: 0.35,
};
